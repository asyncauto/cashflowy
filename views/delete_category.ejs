<div class='ui container'>
	<br>
	<h2>Do you really want to confirm deleting this Budget?</h2>
	This action is permanent. You can't reverse this. All TLIs under this budget will be un-categorized when you delete this category.
	<div class='ui segment'>
		<b>Name:</b> <%=category.name%><br>
		<b>Description:</b> <%=category.description%><br>
		<b>Budget:</b> <%=category.budget%><br>
		<b>Parent:</b> <%=category.parent?category.parent.name:''%><br>
		<b>Number of sub-budgets:</b><%=children.length%><br>
		<b>TLIs with this category:</b><b style='color:red'><%=tlis_count%></b><br>
		<%if(tlis_count>0){%>
			<b style='color:red'>On deletion of this budget, all TLIs with this budget will become un-categorized. </b>
		<%}%>
		<br>
		<br>
		<br>

		<form class="ui form " action="" method='post'>
			<!-- <h4 class="ui dividing header">Shipping Information</h4> -->
			<div class="field">
				<div class="ui checkbox">
					<input name="confirm" type="checkbox">
					<label>Confirm delete</label>
				</div>
			</div>
			<a href="/transactions" class='ui button'>Cancel</a>
			<input type="submit" class="ui red <%-(children.length>0)?'disabled':''%> button" value="Delete">
			<%if(children.length>0){%>
				This budget can't be deleted as it has sub-budgets. Delete the sub-budgets first.
			<%}%>
		</form>
	</div>
</div>
</script>

<script type="text/javascript">
	$(document).ready(function(){
		console.log('hi');
		var a = moment('<%=category.createdAt%>');
		$('#date').text(a.format('DD-MMM-YYYY h:mm a'))
	})

</script>
